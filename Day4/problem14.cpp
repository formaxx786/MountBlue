// Subarray Division

/*
int birthday(vector<int> s, int d, int m) {
     int c=0;
     for(int i=0;i<=s.size()-m;i++){
        int x=accumulate(s.begin()+i,s.begin()+i+m,0);
        if(x==d)
        c++;
     }
     return c;
}
*/