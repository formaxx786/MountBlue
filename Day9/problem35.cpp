// Marc's Cakewalk
/*
long marcsCakewalk(vector<int> calorie) {
sort(calorie.rbegin(),calorie.rend());
long sum=0;
for(long i=0;i<calorie.size();i++){
    long p=pow(2,i);
    sum+=(p*calorie[i]);
}
return sum;
}
*/